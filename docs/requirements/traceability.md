# Assessment requirement traceability

Status terms and the fresh 2026-09-02 account-free results are recorded in [evidence status](../evidence/status.md). A static success does not promote its live counterpart. The table's current status is therefore the live requirement status: until an authorized deployment is recorded, all cloud behavior remains `deployment-evidence-pending`.

| Assessment requirement | Implementation of record | Account-free validation | Future live-only verification | Evidence destination | Current status |
| --- | --- | --- | --- | --- | --- |
| Two GKE clusters in `us-central1` and `us-east1` | `infra/platform/clusters.tf`, `infra/platform/locals.tf` | `make validate-terraform` | `./scripts/verify.sh smoke` checks both clusters `RUNNING` and Fleet memberships `READY` | Cluster/Fleet evidence record | `deployment-evidence-pending` |
| VPC-native private regional networking, subnets, NAT | `infra/foundation/network.tf`, `nat.tf`; `infra/platform/clusters.tf` | `make validate-terraform` | Inspect exact network, endpoints, routes, and controlled egress | Network evidence record | `deployment-evidence-pending` |
| App A and App B in both clusters | `k8s/base/app-a`, `k8s/base/app-b`, regional overlays | `make validate-kubernetes` | Smoke rollout and Ready checks in both regions | Workload evidence record | `deployment-evidence-pending` |
| Three-replica baseline and HPA | Deployments and `k8s/base/app-{a,b}/hpa.yaml` | `make validate-kubernetes` | Smoke proves six Ready replicas per application; optional `verify.sh hpa` proves controlled scale and restoration | Workload record; optional drill record | `deployment-evidence-pending` |
| Global routing with MCI/MCS | `infra/platform/multicluster_features.tf`, `k8s/multicluster/base`, config overlays | `make validate-terraform && make validate-kubernetes` | Smoke verifies MCI VIP, two MCS objects, backend health, and both routes | MCI/backend and endpoint records | `deployment-evidence-pending` |
| Regional failover | Two MCS backends and `./scripts/verify.sh failover` | `make validate-shell` | Optional confirmed application exercise; five consecutive successful global route checks and restoration | Optional failover record | `deployment-evidence-pending` |
| Cloud Armor | `infra/foundation/armor.tf`; both BackendConfigs | `make validate-terraform && make validate-kubernetes` | Smoke checks expected policy attachment; separately review controller backends and policy behavior | Security/backend record | `deployment-evidence-pending` |
| Workload Identity and Secret Manager | `infra/foundation/workload_iam.tf`, `secrets.tf`; SecretProviderClasses | `make validate-terraform && make validate-kubernetes` | Verify mounted files without printing values and verify denied cross-secret access | Redacted IAM/secret record | `deployment-evidence-pending` |
| Centralized logs in BigQuery | `infra/foundation/logging_bigquery.tf`; seven SQL files | `make validate-terraform` plus SQL review | Smoke waits for tables and dry-runs SQL; run bounded queries and redact output | BigQuery record | `deployment-evidence-pending` |
| Grafana dashboard/export | `k8s/base/grafana`; three committed JSON exports | `make validate-grafana && make validate-kubernetes` | Smoke checks one Ready Grafana Pod and three provisioned exports; optional screenshot after redaction | Grafana export or screenshot record | `deployment-evidence-pending` |
| Four overview panels | `assessment-overview.json`: error rate, restarts, latency percentiles, CPU/memory | `make validate-grafana` and JSON review | Open live dashboard and confirm datasource results | Grafana record | `deployment-evidence-pending` |
| Terraform reproducibility | `infra/bootstrap`, `infra/foundation`, `infra/platform` and lock files | `make validate-terraform` | Authorized bootstrap/deploy outputs and state-backed redeploy | Workflow/state-boundary record | `deployment-evidence-pending` |
| Keyless OIDC workflows | `infra/bootstrap/wif.tf`; `.github/workflows/{deploy,teardown}.yml` | `make validate-workflows` and manual subject/permissions review | Authorized workflow URL and GCP audit evidence after redaction | Delivery/identity record | `deployment-evidence-pending` |
| Accessible application endpoint | HTTP routes in MCI and `./scripts/verify.sh smoke` | `make validate-kubernetes && make validate-shell` | 2xx for `/app-a` and `/app-b` by IP; optional DNS/TLS evidence | Endpoint record | `deployment-evidence-pending` |
| Troubleshooting scenario | [troubleshooting guide](../operations/troubleshooting.md), including planned readiness-probe exercise | Documentation review | Execute only under authorization; record symptom, diagnosis, correction, recovery, prevention | Troubleshooting record | `deployment-evidence-pending` |
| Security and limitations | `docs/security`, network policies, Pod security contexts, pinned digests | `make validate` and optional advisory `make security-scan` | Runtime IAM, secret, network, image, and policy verification | Security record | `deployment-evidence-pending` |
| Cost controls and billable services | [cost guide](../cost.md), requests, 30-day partitions, bounded queries, manual lifecycle | Documentation/Terraform review | Billing estimate before deploy and billing report after teardown | Cost/residual record | `deployment-evidence-pending` |
| Setup and deployment | `scripts/bootstrap.sh`, `configure-github-variables.sh`, `deploy.sh`; setup guides | `make validate` | Authorized manual deploy workflow from `main` | Deployment workflow record | `deployment-evidence-pending` |
| Guarded reverse-order teardown and redeploy | `scripts/teardown.sh`, `teardown.yml`, retained bootstrap boundary | `make validate-shell && make validate-workflows` | Exact confirmation, cleanup/residual report, then optional redeploy | Teardown/residual and redeploy records | `deployment-evidence-pending` |

The supplied assessment's free-tier note is treated as permission to omit unavailable features, not as a claim that this two-region architecture is free. Current pricing assumptions are documented in [cost.md](../cost.md).

apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: app-a-secrets
  namespace: assessment
spec:
  parameters:
    secrets: |
      - resourceName: "projects/${GCP_PROJECT_NUMBER}/secrets/app-a-demo/versions/latest"
        path: "app-a-demo"
  provider: gke
